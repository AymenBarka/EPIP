# Security Metrics

Security metrics are deterministic integer counters. Coverage is represented in
basis points from `0` through `10000`, avoiding floating-point ambiguity.

The official report includes contracts, boundaries, policies, violations,
incidents, diagnostics, audit entries, adoption coverage, runtime coverage, and
policy coverage. Metrics describe supplied evidence only; they do not claim that
an unobserved runtime is secure.
